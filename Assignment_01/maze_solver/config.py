# -*- coding: utf-8 -*-
"""ค่าคงที่ทั้งหมดของโปรแกรม - แก้ค่าที่นี่ที่เดียว

โมดูลอื่นอ้างค่าเหล่านี้ผ่านตัวโมดูล (``config.CELL_SIZE_M``) ไม่ใช่นำเข้าชื่อ
ไปเก็บไว้เอง เพื่อให้การแก้ค่าตอนรัน - ซึ่งชุดเทสต์ทำตลอด - มีผลกับทุกโมดูล
ทันทีเหมือนตอนที่ทุกอย่างยังอยู่ไฟล์เดียวกัน
"""
import sys
from collections import namedtuple

# นำเข้าครบทั้งสี่ทิศ แม้ค่าปัจจุบันจะยังไม่ได้ใช้ทุกตัว เพราะไฟล์นี้คือไฟล์ที่
# ผู้ใช้แก้ค่าเอง การเปลี่ยนทิศใน AIM_SEQUENCE ต้องมีชื่อทิศให้ใช้ครบตั้งแต่แรก
from .directions import EAST, NORTH, SOUTH, WEST  # noqa: F401


#: ขั้นตอนหนึ่งของการเล็งเป้าก่อนวางของ ดูวิธีตั้งค่าที่ ``AIM_SEQUENCE``
#:
#: เก็บเป็น "โจทย์" (ของต้องไปตกห่างกำแพงไหนเท่าไร) ไม่ใช่ "คำตอบ" (ToF ต้อง
#: อ่านได้เท่าไร) ค่า ToF เป้าหมายคำนวณเอาตอนรันด้วย ``aim_tof_target_mm()``
#:
#: Attributes:
#:     face (int or None): ทิศที่ต้องหันก่อนวัด None = ใช้ทิศที่มาถึงเลย
#:     ref (int): ทิศของกำแพงที่ ``target_mm`` นับระยะจาก ต้องอยู่แกนเดียวกับ
#:         ``face`` คือเป็นกำแพงที่หันใส่เอง หรือกำแพงตรงข้ามก็ได้
#:     target_mm (float or None): ระยะที่ "ของ" ต้องห่างจากกำแพง ``ref``
#:         None = หันอย่างเดียวไม่จัดระยะ
#:     span_mm (float): ระยะระหว่างกำแพง ``ref`` กับกำแพงตรงข้ามมัน (ความกว้าง
#:         ห้องตามแกนนี้) ใช้เฉพาะตอน ``ref`` เป็นกำแพงคนละด้านกับที่หันใส่
#:     offset_mm (float): ค่าชดเชยที่วัดด้วยตลับเมตรแล้วป้อนกลับเข้ามา รวมทุก
#:         อย่างที่โมเดลไม่ได้คิดถึงไว้ในตัวเดียว (ToF เลยเป้าตอนเบรก จุดหมุน
#:         เลื่อนตอนหมุนตัว ความหนากำแพง) บวกเข้ากับค่า ToF เป้าหมายตรง ๆ
AimStep = namedtuple("AimStep", "face ref target_mm span_mm offset_mm")
AimStep.__new__.__defaults__ = (None, None, 0.0)

# ---------- ขนาดสนามและเป้าหมาย ----------
MAZE_W = 5                      # จำนวนช่องแกน X (ทิศตะวันออกเป็นบวก)
MAZE_H = 6                      # จำนวนช่องแกน Y (ทิศเหนือเป็นบวก)
CELL_SIZE_M = 0.60              # ความกว้าง 1 ช่อง หน่วยเมตร
START_CELL = (0, 0)
START_HEADING = 0                # 0=North 1=East 2=South 3=West
# ช่องที่หุ่น "ไปยืน" ตอนจบ ไม่ใช่ช่องที่ของไปอยู่ สองอย่างนี้เป็นคนละช่องกัน
# เมื่อจุดวางไม่ได้อยู่กลางช่อง เพราะแขนยื่นตรงไปข้างหน้าอย่างเดียว หุ่นจึงต้อง
# ยืนถอยออกมาหนึ่งช่องแล้วเอื้อมเข้าไปวาง (ดู AIM_SEQUENCE)
GOAL_CELLS = [(4, 4)]           # รองรับหลายช่อง เช่นโซนกลาง 2x2 ของ micromouse

# ช่องที่ให้เดินกลับไปหลังวางของเสร็จ None = วางแล้วจบตรงนั้นเลยไม่ต้องเดินกลับ
# (สั่งชั่วคราวด้วย --no-return ได้โดยไม่ต้องแก้ไฟล์)
#
# ขากลับไม่ใช่โค้ดคนละชุด เป็นการตั้ง maze.goals ใหม่แล้วปล่อยให้ลูปเดิมทำงานต่อ
# บนแผนที่ก้อนเดิม สิ่งที่ต่างจากขาไปมีอย่างเดียวคือหุ่นไม่ได้จอดอยู่กลางช่อง
# เป๊ะ ๆ ตอนเริ่ม เพราะ AIM_SEQUENCE เพิ่งเลื่อนมันไปเล็งจุดวาง ระยะที่เยื้องนี้
# ติดตัวไปทุกช่องเท่า ๆ กัน (advance_one_cell นับ odometry เป็นช่วง ไม่ใช่พิกัด
# สัมบูรณ์) จึงไม่สะสมขึ้นเรื่อย ๆ และหายไปเองในช่องแรกที่ ToF เบรกให้ที่กำแพง
RETURN_CELL = START_CELL

# ของที่เพิ่งวางกองอยู่บนพื้นในทิศที่ยื่นแขนวาง ซึ่งเป็นสิ่งกีดขวางที่เซนเซอร์
# มองไม่เห็น - ToF กับ Sharp ติดอยู่ในระดับกำแพง ส่วนวัตถุเตี้ยกว่านั้นมาก
# หุ่นจึงต้องจดเองว่าวางไว้ตรงไหน ไม่ใช่รอให้เซนเซอร์เจอ ไม่งั้นขากลับที่ Flood
# Fill เลือกให้จะพาไปเดินทับของตัวเอง (ทิศที่วางกับทิศที่สั้นที่สุดกลับบ้านมักจะ
# เป็นทิศเดียวกัน เพราะการวางของเล็งให้ห่างกำแพงมากที่สุด = เล็งเข้ากลางสนาม)
#
# True  = กันทั้ง "ขอบที่ยื่นแขนข้ามไป" และ "ช่องถัดไปทางนั้น" ค่าเริ่มต้น เพราะ
#         ARM_PLACE_XY ยื่นไป 220 mm จากฐานแขน บวกกับตัวหุ่นแล้วของไปตกคร่อม
#         เส้นแบ่งช่องพอดี (ครึ่งช่อง = 300 mm) การกันแค่ขอบจึงไม่พอ หุ่นยังอ้อม
#         ไปเข้าช่องนั้นทางด้านอื่นแล้วเบียดของอยู่ดี
# False = กันแค่ขอบที่ยื่นแขนข้ามไป ใช้เมื่อยื่นแขนสั้นจนของอยู่ในช่องแน่ ๆ
PLACED_OBJECT_BLOCKS_NEXT_CELL = True

# ขากลับให้เดินเฉพาะด้านที่ "ตรวจแล้วว่าโล่งจริง" เท่านั้นหรือไม่
#
# ขาไป Flood Fill ถือว่าด้านที่ยังไม่เคยเห็นเป็นทางเปิดไว้ก่อน ซึ่งถูกแล้วเพราะ
# หน้าที่ของขาไปคือสำรวจ เจอกำแพงก็มาร์กแล้วหาทางใหม่ แต่ขากลับไม่มีอะไรให้
# ค้นพบอีกแล้ว มีแต่ของที่เพิ่งวางไว้ให้ระวัง การเดาว่าเปิดจึงมีแต่เสีย
#
# True  = ขากลับใช้เฉพาะขอบที่ observe() ยืนยันแล้ว ซึ่งรวมทุกขอบที่หุ่นเดินผ่าน
#         มาเอง (ก่อนข้ามขอบไหน หุ่นมองเห็นมันเป็นด้านหน้ามาแล้วเสมอ) ทางกลับ
#         จึงมีอย่างน้อยเท่ากับเส้นทางขาไป และลัดได้ถ้าเคยเห็นช่องเปิดด้านข้าง
#         ระหว่างทาง ถ้าความรู้ที่มีกลับไม่ถึงจริง ๆ จะถอยไปใช้แบบเดาว่าเปิด
#         อัตโนมัติ พร้อมพิมพ์บอก
# False = ขากลับใช้ตรรกะเดียวกับขาไปทุกอย่าง (พฤติกรรมก่อนมีฟีเจอร์นี้)
RETURN_KNOWN_ONLY = True

# ---------- การต่อสายเซนเซอร์ ----------
TOF_INDEX = 0                   # sub_distance คืน list 4 ตัว ใช้ตัวไหนเป็นด้านหน้า
SHARP_LEFT = (2, 1)             # (hub_id, port) อ่านด้วย ADC
SHARP_RIGHT = (1, 1)            # (hub_id, port) อ่านด้วย ADC
IR_LEFT_45 = (4, 1)             # (hub_id, port) อ่านด้วย IO (digital 0/1)
IR_RIGHT_45 = (3, 1)            # (hub_id, port) อ่านด้วย IO (digital 0/1)

# ---------- ค่าที่ได้จาก `--calib` เท่านั้น ห้ามเดา ----------
# ตราบใดที่ยังเป็น None โปรแกรมจะปฏิเสธที่จะวิ่งในสนามจริง
# เหตุผล: threshold ที่ผิดทำให้หุ่น "วิ่งดูปกติทุกอย่างแต่สร้างแผนที่ผิด"
# ซึ่งแยกไม่ออกจากบั๊กของ odometry หรือของ flood fill ตอนอยู่หน้าสนาม
SHARP_LEFT_WALL_ADC = (372, 358)      # (enter, exit) ทำ hysteresis กันค่ากระพริบ
SHARP_RIGHT_WALL_ADC = (442, 430)    # (enter, exit)
SHARP_LEFT_REF = 368     # ค่า ADC ซ้าย ตอนหุ่นอยู่กลางช่องพอดี
SHARP_RIGHT_REF = 436          # ค่า ADC ขวา ตอนหุ่นอยู่กลางช่องพอดี
IR_TRIGGERED_VALUE = 0       # ค่า IO ตอนมีสิ่งกีดขวาง (0 หรือ 1)
FRONT_STOP_MM = 65         # ToF ที่อ่านได้ตอนหุ่นอยู่กลางช่องและหันชนกำแพง
# ระยะที่หัว ToF ล้ำหน้าจุดหมุนของหุ่น (mm) None = อนุมานจาก FRONT_STOP_MM
# ให้เอง ซึ่งถูกตามนิยาม: จุดหมุนห่างขอบช่องครึ่งช่อง ToF อ่านได้ FRONT_STOP_MM
# ส่วนที่หายไปจึงเป็นความยาวของหัวเซนเซอร์ที่ยื่นล้ำจุดหมุนออกไป
# ตั้งทับเมื่อวัดเองแล้วไม่ตรง ซึ่งแปลว่า FRONT_STOP_MM เองก็คลาดด้วย
TOF_FORWARD_OFFSET_MM = None

FRONT_WALL_MM_OVERRIDE = None   # ปกติปล่อย None ให้คำนวณจากเรขาคณิตของช่อง

# ---------- เกณฑ์ตรวจกำแพง ----------
TOF_MAX_VALID_MM = 4000         # เกินนี้ถือว่าอ่านไม่ได้ / ไม่มีอะไรอยู่ในระยะ
WALL_VOTE_SAMPLES = 5           # อ่านกี่ครั้งตอนจอดนิ่ง ก่อนโหวตลงแผนที่
WALL_VOTE_INTERVAL = 0.06
# อ่าน ToF กี่ครั้งตอนจอดนิ่ง แล้วเอามัธยฐาน ใช้ตอนที่ต้องการ "ระยะ" จริง ๆ
# (จัดระยะ / ตรวจกับแผนที่) ไม่ใช่แค่ "มีกำแพงไหม" ซึ่งใช้ WALL_VOTE_SAMPLES
# มัธยฐานทนค่าโดด ๆ ได้ดีกว่าค่าเฉลี่ย ซึ่งสำคัญเพราะ ToF ให้ค่าหลุดเป็นครั้งคราว
TOF_SETTLE_SAMPLES = 5

# ---------- IR 45 องศา ----------
# ใช้เป็นตัวกันชนจุดบอดทแยงหน้า (Sharp ยิงตรงข้าง ToF ยิงตรงหน้า มุมทแยงไม่มีใครดู)
# ไม่ใช้เป็นตัวตัดสินใจว่ามีกำแพงหรือไม่ และไม่ใช้จบการเคลื่อนที่
USE_IR_AS_GUARD = True          # ติด -> ชะลอความเร็ว + เบี่ยงหนีเล็กน้อย
USE_IR_AS_SLANT = False         # ติดข้างเดียว -> เดาว่าเข้าช่องเอียง แล้วแก้ yaw
IR_SLANT_BIAS_DEG = 4.0         # องศาที่เอียงเป้าหมายหนีเมื่อเปิด USE_IR_AS_SLANT
GUARD_STRAFE = 0.05             # m/s เบี่ยงหนีเมื่อ IR guard ติดข้างเดียว

# ---------- ความเร็ว ----------
BASE_SPEED = 0.25               # m/s เดินหน้าปกติ
SLOW_SPEED = 0.09               # m/s ตอนเข้าใกล้กำแพงหน้า
GUARD_SPEED = 0.07              # m/s ตอน IR guard ติด
BACKUP_SPEED = 0.12             # m/s ตอนถอยกลับเข้าช่องเดิม
ALIGN_SPEED = 0.08              # m/s ตอนจัดระยะกับกำแพงหน้า
TURN_MAX_DPS = 55.0             # deg/s ความเร็วหมุนสูงสุด
TURN_MIN_DPS = 14.0             # deg/s กำลังขับขั้นต่ำเพื่อชนะแรงเสียดทานพื้น

# ---------- ตัวคุม (controller) ----------
# หมายเหตุ: drive_speed ปัดค่าก่อนส่งเสมอ (util.py val2proto) โดย x/y เหลือทศนิยม
# 2 ตำแหน่ง และ z ถูกปัดเป็นจำนวนเต็ม ดังนั้นคำสั่งที่เล็กกว่า 0.01 m/s หรือ
# 1 deg/s จะกลายเป็นศูนย์ การจูนเกนให้ผลลัพธ์ต่ำกว่าขั้นนี้จึงไม่มีผลอะไรเลย
KP_TURN = 1.5                   # deg/s ต่อ 1 องศา error ตอนหมุนอยู่กับที่
TURN_TOLERANCE_DEG = 1.5
TURN_TIMEOUT_S = 5.0
KP_YAW_HOLD = 0.9               # deg/s ต่อ 1 องศา error ตอนเดินหน้า
MAX_YAW_CORRECT_DPS = 16.0
YAW_HOLD_DEADBAND_DEG = 1.2
KP_CENTER = 0.0006              # m/s ต่อ 1 หน่วย ADC error (ไม่แปลงเป็น cm)
CENTER_DEADBAND_ADC = 15        # error ต่ำกว่านี้ถือว่ากลางช่องแล้ว
MAX_STRAFE = 0.08               # m/s strafe สูงสุด
ALIGN_TOLERANCE_MM = 18
# จัดระยะซ้ำได้กี่รอบ ก่อนยอมรับตำแหน่งที่ได้
#
# รอบเดียวไม่พอเพราะ _travel หยุดตอนค่า ToF "ข้ามเกณฑ์" ไม่ใช่ตอนถึงเป้าพอดี
# ระหว่างนั้นมีดีเลย์ของ DDS (1/DDS_FREQ วินาที) บวกคาบ control loop บวกระยะที่
# หุ่นไหลต่อหลังสั่งหยุด รวมแล้วเลยเป้าไปทางเดียวเสมอไม่กี่มิลลิเมตร รอบที่สอง
# วัดตอนจอดนิ่งด้วยค่ามัธยฐานแล้วเก็บส่วนที่เหลือ
#
# 2 พอสำหรับความคลาดเคลื่อนที่เป็นระบบแบบนี้ เพราะรอบที่สองขยับสั้นมากจนดีเลย์
# แทบไม่มีผล ตั้งสูงกว่านี้คือยอมเสียเวลาไล่จับ noise ของเซนเซอร์ที่ไม่มีวันจบ
ALIGN_MAX_PASSES = 2

# ยอมให้ค่า ToF ที่วัดได้ ห่างจากค่าที่คาดไว้ได้กี่ mm ก่อนจะไม่เชื่อ
#
# ใช้สองที่ ทั้งตอนเล็งเป้าและตอนเดินสำรวจ กันคนละเคสแต่เป็นความผิดพลาดชนิด
# เดียวกัน คือ "ค่าที่ดูสมเหตุสมผลแต่ผิด" - ตอนเล็งคือลำแสงเล็ดลอดผ่านประตูไป
# โดนกำแพงห้องถัดไป ตอนเดินสำรวจคือหุ่นนับช่องพลาดจนคิดว่าตัวเองอยู่คนละที่
#
# ครึ่งช่องคือขอบเขตธรรมชาติ เพราะถ้าหุ่นอยู่ในช่องที่ถูกต้องจริง ตำแหน่งมันจะ
# ผิดจากกลางช่องได้ไม่เกินครึ่งช่องตามนิยาม ค่าที่หลุดไปกว่านั้นแปลว่าหุ่นอยู่
# คนละช่องกับที่คิด หรือกำลังมองอย่างอื่นที่ไม่ใช่กำแพงที่ตั้งใจวัด
TOF_SANITY_WINDOW_MM = None     # None = ใช้ครึ่งช่อง

# ---------- การนับช่อง ----------
CELL_COMPLETE_RATIO = 0.85      # เดินได้ >= 85% ของช่อง = ถือว่าเข้าช่องใหม่แล้ว
TOF_STOP_MIN_RATIO = 0.50       # ถ้าจบเพราะ ToF ต้องเดินมาแล้วอย่างน้อย 50%
MOVE_TIMEOUT_RATIO = 2.0        # timeout = (CELL_SIZE_M / BASE_SPEED) * ค่านี้

# ---------- payload (คีบ / วางวัตถุ) ----------
DO_PAYLOAD = True               # ปิดชั่วคราวได้ตอนดีบักเฉพาะการเดิน
# ท่าแขนทุกค่าในไฟล์นี้ต้องอยู่ในช่วง x 0-220 mm, y 0-150 mm (ดูหมายเหตุ SDK
# ข้อ 7 ใน maze_solver/__init__.py) y = 0 คือต่ำสุดติดพื้น ไม่มีค่าติดลบ
ARM_CARRY_XY = (0, 150)        # ตำแหน่งแขนตอนวิ่ง (mm) ต้องไม่บัง ToF
ARM_PLACE_XY = (220, 0)         # ตำแหน่งแขนตอนวางของ (mm) ยื่นไกลกว่าท่าวิ่ง
                                # ให้ของพ้นตัวหุ่นก่อนปล่อย และ y = 0 คือติดพื้น
# ท่าวางตอนจบงานแบบไม่ถึงเป้าหมาย (เซนเซอร์หลุด / ไปต่อไม่ได้ / Ctrl-C)
# ตั้งให้ยื่นสั้นกว่า ARM_PLACE_XY โดยตั้งใจ เพราะการจบแบบนี้เกิดตอนหุ่นจอดอยู่
# ที่ไหนก็ไม่รู้ - เคส "ไปเป้าหมายไม่ได้แล้ว" กับ "ถูกล้อมทุกด้าน" คือหุ่นจอด
# หันชนกำแพงพอดี ถ้ายื่นสุดแขน 220 mm ของกับนิ้วจะไปกระแทกกำแพงแทนที่จะวางลง
# พื้น ที่ 110 mm ของยังพ้นล้อหน้าแต่ไม่ชนกำแพงที่ห่าง FRONT_STOP_MM
ARM_DROP_XY = (110, 0)

# ระยะจาก "จุดหมุนของหุ่น" ถึง "จุดที่ของไปตกบนพื้น" เมื่อแขนอยู่ท่า ARM_PLACE_XY
# หน่วย mm วัดตามแนวที่หุ่นหันหน้าอยู่
#
# คำนวณจาก ARM_PLACE_XY ไม่ได้ เพราะ x ของแขนนับจากฐานแขน ไม่ใช่จุดหมุนหุ่น และ
# ระยะระหว่างสองจุดนั้นไม่มีใน SDK เลย ต้องวัดเอง: สั่ง --armtest ให้แขนไปท่า
# ARM_PLACE_XY แล้ววางของ จากนั้นวัดจากกึ่งกลางหุ่นถึงกึ่งกลางของด้วยตลับเมตร
#
# ค่านี้เข้าสมการเฉพาะขั้นสุดท้ายของ AIM_SEQUENCE ซึ่งเป็นขั้นที่ยื่นแขนวางจริง
# ขั้นอื่นแขนยื่นตั้งฉากกับแกนที่กำลังจัด ระยะเอื้อมจึงไม่เกี่ยวกับแกนนั้น
#
# 485 ถอดกลับมาจากค่า ToF 650 ที่ฮาร์ดโค้ดไว้เดิม ยังไม่เคยวัดจริง ให้ยืนยันด้วย
# ตลับเมตรหนึ่งครั้งแล้วแก้ตรงนี้ เป็นค่าของ "ตัวหุ่น" ไม่ใช่ของสนาม วัดครั้งเดียว
# ใช้ได้กับทุกสนามตลอดไป
ARM_REACH_MM = 485

# ระยะห่างจากกำแพง "ด้านหน้า" ที่ต้องการก่อนยื่นแขนวางของที่ช่องเป้าหมาย (mm)
# None = ไม่ต้องถอย ยื่นแขนวางจากตรงที่หุ่นจอดเลย
#
# มีไว้เพราะตอนเข้าช่องเป้าหมาย ToF เบรกให้ที่ FRONT_STOP_MM ซึ่งใกล้กำแพงกว่า
# ระยะที่แขนยื่นออกไป (ARM_PLACE_XY) มาก วางจากตรงนั้นของกับนิ้วจะกระแทกกำแพง
# แทนที่จะลงพื้น การถอยคือวิธีให้แขนมีที่ยื่นโดยไม่ต้องหดระยะวางให้สั้นลง
#
# ข้อจำกัดที่ต้องรู้ก่อนตั้งค่า
# 1. บังคับได้เฉพาะกำแพงหน้า เพราะมีแต่ ToF ที่คืนค่าเป็นมิลลิเมตรจริง Sharp
#    ข้างคืนแค่ ADC ดิบที่บอกได้ว่า "มีกำแพงไหม" กับ "ชิดกว่าหรือห่างกว่าจุด
#    กึ่งกลางช่อง" แปลงเป็นระยะไม่ได้ ด้านข้างจึงทำได้แค่ประคองให้อยู่กลางช่อง
#    ระหว่างถอย ซึ่งก็คือระยะห่างด้านข้างที่มากที่สุดเท่าที่ช่องกว้างเท่านี้จะให้ได้
# 2. ในช่องกว้าง CELL_SIZE_M เมตร กำแพงซ้ายขวาห่างกันแค่นั้น ค่าที่เกินครึ่งช่อง
#    จึงเป็นไปไม่ได้อยู่แล้วสำหรับด้านข้าง ต่อให้หุ่นเป็นจุดเดียว
# 3. หุ่นถอยได้ไม่เกินระยะที่เพิ่งเดินเข้าช่องนี้มา (พื้นที่ที่ยืนยันแล้วว่าโล่ง)
#    ถ้าเป้าหมายคือช่องเริ่มต้นที่ไม่ได้เดินมา จะไม่ถอยเลย
GOAL_WALL_CLEARANCE_MM = 400

# ---------- การเล็งเป้าก่อนวางของ ----------
# ใช้เมื่อจุดวางถูกกำหนดเป็น "ระยะจากกำแพง" ไม่ใช่ "กลางช่อง" ซึ่งกลไกเดินทีละ
# ช่องเล็งให้ไม่ได้ ตั้งเป็น [] = ไม่เล็ง ถอยตาม GOAL_WALL_CLEARANCE_MM แล้ววาง
# จากตรงที่จอด (พฤติกรรมก่อนมีฟีเจอร์นี้) ถ้าตั้ง AIM_SEQUENCE ไว้ ค่า
# GOAL_WALL_CLEARANCE_MM จะถูกข้ามไปเอง เพราะการเล็งคุมระยะได้ละเอียดกว่า
#
# แต่ละขั้นเป็น AimStep ที่บอก "โจทย์" คือของต้องไปตกห่างกำแพงไหนเท่าไร ส่วน
# "ToF ต้องอ่านได้เท่าไร" เป็นสิ่งที่ aim_tof_target_mm() คำนวณให้ตอนรัน
#
#   AimStep(face=ทิศที่หันก่อนวัด,  None = ใช้ทิศที่มาถึงเลย ไม่ต้องหัน
#           ref=ทิศของกำแพงที่นับระยะเป้าจาก,
#           target_mm=ระยะที่ "ของ" ต้องห่างกำแพงนั้น, None = หันเฉย ๆ ไม่จัดระยะ
#           span_mm=ความกว้างห้องตามแกนนี้, ใช้เมื่อ ref ไม่ใช่กำแพงที่หันใส่
#           offset_mm=ค่าชดเชยจากตลับเมตร)
#
#   ทิศของขั้นสุดท้ายคือทิศที่ยื่นแขนวาง เพราะแขนยื่นตรงไปข้างหน้าอย่างเดียว
#   และเป็นขั้นเดียวที่ ARM_REACH_MM เข้าสมการ
#   ห้ามใส่ทิศที่ ToF มองไม่เห็นกำแพง (เช่นด้านที่เป็นประตูเข้าห้อง)
#
# ทำไมไม่ฮาร์ดโค้ดค่า ToF ตรง ๆ เหมือนเดิม: ตัวเลขอย่าง 560 เกิดจากสนามเดียวและ
# ผูกกับ FRONT_STOP_MM ที่ใช้ตอนจูน แก้ FRONT_STOP_MM เมื่อไรมันก็ผิดทันทีโดยไม่
# มีอะไรเตือน ส่วน ARM_REACH_MM กับ TOF_FORWARD_OFFSET_MM เป็นสมบัติของ "ตัวหุ่น"
# วัดครั้งเดียวใช้ได้ทุกสนาม เหลือแค่ ref/target_mm/span_mm ที่เป็นของสนามซึ่ง
# อ่านจากโจทย์ได้ตรง ๆ ไม่ต้องจูน
#
# ยังต้องจูนอยู่บ้าง: offset_mm รวมทุกอย่างที่โมเดลไม่ได้คิดถึง (ToF เลยเป้าตอน
# เบรก จุดหมุนเลื่อนตอนหมุนตัว ความหนากำแพง) วิธีหา - รันหนึ่งรอบ วัดด้วยตลับเมตร
# ว่าของไปตกห่างกำแพงเท่าไรจริง แล้ว
#
#   offset_mm ใหม่ = offset_mm เดิม + (วัดได้ - เป้า)   เมื่อ ref เป็นกำแพงที่หันใส่
#   offset_mm ใหม่ = offset_mm เดิม - (วัดได้ - เป้า)   เมื่อ ref เป็นกำแพงตรงข้าม
#
# ค่าด้านล่างคือของสนามที่ห้องเป้าหมายเป็น 2x2 ช่อง (กว้างและสูงด้านละ 1200 mm)
# หุ่นเข้าห้องได้ทางเดียวคือตกลงมาจากทางเหนือเข้าช่อง (4,4) หันใต้ แล้วเป้าอยู่
# ห่างกำแพงเหนือกับกำแพงตะวันตกของห้องด้านละ 400 mm
AIM_SEQUENCE = [
    # หันใต้ วัดกำแพงล่างของห้อง แต่เป้านับจากกำแพงเหนือซึ่งเป็นประตู มองไม่เห็น
    # จึงต้องบอก span_mm ให้พลิกด้านเอา  ToF เป้า = (1200-400) - 235 - 5 = 560
    AimStep(face=SOUTH, ref=NORTH, target_mm=400, span_mm=1200, offset_mm=-5),
    # หันตะวันตก วัดกำแพงตะวันตกของห้องซึ่งเป็นกำแพงที่เป้านับระยะจากพอดี
    # เป็นขั้นสุดท้าย = ขั้นที่ยื่นแขนวาง  ToF เป้า = 400 + 485 - 235 = 650
    AimStep(face=WEST, ref=WEST, target_mm=400, span_mm=1200),
]
# ขยับได้ไกลสุดต่อหนึ่งขั้น กันความเสียหายเมื่อ ToF เพี้ยน
#
# ต้องมากกว่า "ระยะที่ไกลที่สุดที่ขั้นใดขั้นหนึ่งต้องขยับ" ไม่งั้นขั้นนั้นจะจบด้วย
# reason="budget" คือหยุดกลางทางทั้งที่ ToF ยังไม่ถึงเป้า แล้วเล็งเพี้ยนแบบเงียบ ๆ
# (มีแค่บรรทัด [WARN] เตือน ส่วนหุ่นก็หันไปวางต่อตามปกติ)
#
# หาค่าต่ำสุดที่ต้องใช้: หุ่นเข้าช่องเป้าหมายมาโดย ToF เบรกให้ที่ FRONT_STOP_MM
# ขั้นแรกของ AIM_SEQUENCE จึงต้องขยับ |เป้าขั้นแรก - FRONT_STOP_MM|
#   ค่าปัจจุบัน: |560 - 65| = 495 mm -> ต้องไม่ต่ำกว่า 0.495
#
# อย่าคำนวณเลขนี้ด้วยมือแล้วจดไว้เฉย ๆ เพราะเป้าขั้นแรกไม่ใช่ค่าคงที่อีกต่อไป
# มันขยับตาม FRONT_STOP_MM กับ ARM_REACH_MM (ดู aim_tof_target_mm) เทสต์
# test_aim_config_is_usable คำนวณใหม่ทุกครั้งที่รัน แล้วเช็คว่าเหลือขอบพอไหม
#
# เผื่อขอบไว้ด้วย ไม่ใช่ตั้งให้พอดีเป๊ะ: 0.50 เคยพอดีเกินไป (เหลือ 5 mm) ซึ่ง
# แปลว่าคาลิเบรตขยับนิดเดียวขั้นแรกก็จบด้วย [WARN] budget ทันที การขยับเพดานขึ้น
# ไม่ทำให้หุ่นเดินไกลขึ้น เพราะ align_to_wall ใช้ min(ระยะที่ผิดอยู่, เพดาน) อยู่แล้ว
# เพดานเป็นแค่ตัวกันความเสียหายเมื่อ ToF เพี้ยน ซึ่งตอนนี้มีด่านตรวจ
# (tof_sanity_window_mm) ดักไว้อีกชั้นก่อนถึงตรงนี้แล้ว
#
# เพดานบนคือ CELL_SIZE_M เพราะหุ่นถอยไกลกว่าหนึ่งช่องไม่ได้อยู่แล้ว - พื้นที่ที่
# ยืนยันแล้วว่าโล่งมีแค่ช่องที่เพิ่งเดินผ่านมาเมื่อกี้
AIM_MAX_MOVE_M = 0.55

GRIPPER_POWER = 50
GRIPPER_TUCK_POWER = 30         # แรงหุบเบา ๆ ตอนมือเปล่า กันนิ้วเกี่ยวกำแพง
PAYLOAD_LOAD_S = 2.0            # เวลากางกริปเปอร์ค้างไว้ให้วางวัตถุก่อนหุบคีบ
GRIPPER_ACT_S = 1.5             # เวลารอให้นิ้วขยับจนสุด (ไม่มี action ให้รอ)
GRIPPER_RELEASE_TRIES = 3       # สั่งกางซ้ำได้กี่ครั้งตอนปล่อยของ
GRIPPER_STATUS_FREQ = 5         # Hz ของ sub_status สถานะเปลี่ยนช้า ไม่ต้องถี่
GRIPPER_STATUS_STALE_S = 1.5    # เกินนี้ถือว่าสถานะค้างเก่า เอามาตัดสินไม่ได้
ARM_TIMEOUT_S = 6               # timeout ของ action แขนกล (วินาที)
ARM_SETTLE_S = 0.3              # รอให้แขนหยุดสั่นหลังจบ action ก่อนสั่งท่าต่อไป
PLACE_SETTLE_S = 0.5            # รอให้แขนนิ่งที่ท่าวางก่อนกางนิ้ว กันวัตถุล้ม
GRIPPER_TUCK_S = 1.0            # รอให้นิ้วหุบเก็บจนสุดตอนมือเปล่า

# ---------- payload: หยิบที่ไหน และหยิบยังไง ----------
# สองค่านี้ไม่เกี่ยวกัน ผสมกันได้อิสระ ปิดการคีบทั้งหมดด้วย --no-payload
#
# PICK_CELL = ช่องที่หุ่นไปยืนหยิบของ
#   START_CELL  หยิบตรงจุดเริ่มโดยไม่ต้องขยับ (ค่าเริ่มต้น)
#   (1, 0) ฯลฯ  Flood Fill พาไปช่องนั้นก่อน แล้วค่อยตั้งเป้าใหม่ไป GOAL_CELLS
#   ถ้าให้หุ่นวิ่งไปหยิบที่ช่องอื่น ต้องวางของไว้ "กลางช่อง" เท่านั้น เพราะ
#   1. ToF จะเห็นแล้วเบรกให้เองที่ FRONT_STOP_MM ซึ่งเป็นระยะหยิบที่ซ้ำเดิมได้
#   2. ถ้าวางชิดขอบช่อง ToF จะเห็นเป็นกำแพงตั้งแต่ยังอยู่ช่องก่อนหน้า (เกณฑ์ =
#      FRONT_STOP_MM + ครึ่งช่อง) แล้ว Maze จะปิดทางเข้าช่องนั้น "ถาวร" เพราะ
#      set_wall เพิ่มกำแพงได้อย่างเดียว ลบออกไม่ได้ (ดู Maze.set_wall)
#
# ARM_PICK_XY = วิธีหยิบ
#   None        กางกริปเปอร์ค้าง PAYLOAD_LOAD_S วินาทีให้คนวางของใส่มือ
#               ทนต่อการวางคลาดเคลื่อนที่สุด ไม่ต้องจูนอะไรเลย (ค่าเริ่มต้น)
#   (x, y)      ยื่นแขนลงไปหยิบเองที่ท่านั้น ต้องจูนกับของจริงก่อนด้วย --armtest
#               และของต้องอยู่ในระยะเอื้อม (x 0-220 mm, y 0-150 mm)
PICK_CELL = START_CELL          # หยิบตรงจุดเริ่ม ไม่ต้องขยับไปไหน
# ARM_PICK_XY = None              # คนวางของใส่มือให้
# PICK_CELL = (1, 0)            # ให้หุ่นวิ่งไปหยิบเองที่ช่องนี้
ARM_PICK_XY = (220, 0)        # ให้ยื่นแขนหยิบเองที่ท่านี้ (จูนด้วย --armtest)

# แขน EP ไม่มีแกนหมุนซ้ายขวา (moveto รับแค่ x, y) การเล็งจึงต้องอาศัยแชสซีหัน
# ให้ตรงอย่างเดียว ปกติไม่ต้องตั้ง เพราะหุ่นจอดหันหน้าใส่ของอยู่แล้วตอน ToF เบรก
# ตั้งเมื่อรู้ว่าของเยื้องไปทางใดทางหนึ่งของช่องเท่านั้น
PICK_HEADING = None             # ทิศที่ต้องหันก่อนหยิบ (0=N 1=E 2=S 3=W)

# ---------- ระบบ ----------
CONTROL_DT = 0.04               # คาบของ control loop
SETTLE_S = 0.25                 # เวลารอให้หุ่นนิ่งก่อนอ่านค่าลงแผนที่
MAX_STEPS = 200
DDS_FREQ = 20                   # Hz รองรับเฉพาะ 1, 5, 10, 20, 50
ADAPTER_WAIT_S = 2.0            # รอ sub_adapter นานแค่ไหนก่อนสลับไป fallback
SENSOR_STALE_S = 0.5            # ไม่มีข้อมูลใหม่เกินนี้ = ถือว่าค่าใช้ไม่ได้
DRIVE_WATCHDOG_S = 0.5          # หุ่นหยุดเองถ้าเราไม่ส่งคำสั่งใหม่ภายในเวลานี้
YAW_SIGN_TEST_DPS = 40.0        # ความเร็วหมุนตอนหาเครื่องหมายของ yaw
YAW_SIGN_TEST_S = 0.45          # นานแค่ไหน (~18 องศา)
CONN_TYPE = "ap"                # ap / sta / rndis


# ---------- โหมด --armtest ----------
ARMTEST_TOF_SAMPLES = 20        # จำนวนครั้งที่อ่าน ToF ตอนเช็คว่าของบังหรือไม่

def require_calibration():
    """ตรวจว่าค่าจาก --calib ครบแล้ว ถ้าไม่ครบให้หยุดพร้อมบอกว่าขาดตัวไหน"""
    missing = [name for name in (
        "SHARP_LEFT_WALL_ADC", "SHARP_RIGHT_WALL_ADC",
        "SHARP_LEFT_REF", "SHARP_RIGHT_REF",
        "IR_TRIGGERED_VALUE", "FRONT_STOP_MM",
    ) if globals()[name] is None]
    if not missing:
        return
    print("\n[STOP] ยังไม่ได้คาลิเบรตเซนเซอร์ ค่าที่ยังขาด:")
    for name in missing:
        print("         - {0}".format(name))
    print("\n  รัน `python test_code.py --calib` ก่อน (ใช้เวลาไม่ถึง 2 นาที)")
    print("  แล้วคัดลอกบล็อกค่าที่มันพิมพ์ออกมา ไปวางทับในไฟล์นี้ (maze_solver/config.py)")
    print("\n  เหตุผลที่ไม่ให้รันด้วยค่าเดา: threshold ที่ผิดจะทำให้หุ่นวิ่งดูปกติ")
    print("  ทุกอย่างแต่สร้างแผนที่ผิด ซึ่งหน้าสนามจะแยกไม่ออกเลยว่าปัญหาอยู่ที่")
    print("  threshold, odometry หรือ flood fill\n")
    sys.exit(1)
